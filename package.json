{
  "name": "pi-hubble",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "vitest run --exclude=test/integration/**",
    "test:integration": "vitest run test/integration/pi-hubble.integration.test.ts",
    "lint": "oxlint .",
    "typecheck": "tsc --noEmit",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "check": "npm run format && npm run lint",
    "sync:skills": "node ./scripts/sync-hubble-skills.ts",
    "check:skills": "node ./scripts/sync-hubble-skills.ts --check"
  },
  "pi": {
    "extensions": [
      "./extensions/hubble.ts"
    ],
    "skills": [
      "./skills/create-html-app"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@oxlint/plugins": "^1.80.0",
    "@types/node": "^26.2.0",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typebox": "^1.3.20",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.3",
    "@earendil-works/pi-tui": "^0.84.2",
    "better-result": "3.0.1"
  }
}
